const names = ["Maria", "João", "Eduardo", "Gabriel", "Julia"];

// names.splice(index atual, delete, add(element)1,add(element)2);

// pop:
// const removeNames = names.splice(-1, 1);

// shift:
// const removeNames = names.splice(0, 1);

// push:
// const addNames = names.splice(names.length, 0, "Luiz");

// unshift:
// const addNames = names.splice(0, 0, "Luiz");

// console.log(names, addNames);
