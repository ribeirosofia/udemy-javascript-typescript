//Escreva uma função chamada landscapeImg que
// recebe dois argumentos, largura e altura
// de uma imagem (number).
// Retorne true se a imagem estiver no modo
// paisagem.

const landscapeImg = (width, height) => width > height;

console.log(landscapeImg(1080, 1920));
